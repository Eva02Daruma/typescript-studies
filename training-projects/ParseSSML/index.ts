/// parseSSML: Implement a SSML Parser that takes a SSML string and returns a SSML Node Tree.
//Do NOT use or refer to any pre-existing XML parsing libraries.

// TODO : Research how to code an ssml parser to return a node tree without using external librarys

function SSMLparser(SSML:string):undefined{
    // returns a SSML Node Tree
    
    
}